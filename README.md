### Usage

run in terminal to test:
```
javac lm.java
java lm Book1.CSV
```

it works by minimizing the partial derivatives of the cost funtion, the cost function is a measure for the RSS given the two parameters ``theta_0`` and ``theta_1``
